name: DLL Builder

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-dlls:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      # 1️⃣ Checkout code
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # 2️⃣ Set Git user
      - name: Set Git user
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"

      # 3️⃣ Setup .NET 6
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '6.0.x'

      # 4️⃣ Define paths (adjustable)
      - name: Set paths
        run: |
          echo "INPUT_PATH=DLL-Builder/input/stardew" >> $GITHUB_ENV
          echo "GAME_PATH=DLL-Builder/input/stardew" >> $GITHUB_ENV
          echo "OUTPUT_PATH=DLL-Builder/output/build" >> $GITHUB_ENV
          echo "SLN_FILE=" >> $GITHUB_ENV  # leave empty to auto detect

      # 5️⃣ Create output folder
      - name: Prepare output folder
        run: mkdir -p ${{ env.OUTPUT_PATH }}

      # 6️⃣ Run build for all SLNs (auto-detect if empty)
      - name: Build DLLs
        run: |
          # Find SLN if not set
          if [ -z "${SLN_FILE}" ]; then
            SLN_FILE=$(find "${INPUT_PATH}" -name "*.sln" | head -n 1)
            if [ -z "$SLN_FILE" ]; then
              echo "❌ No solution file found in ${INPUT_PATH}"
              exit 1
            fi
          else
            SLN_FILE="${SLN_FILE}"
          fi
          echo "🟢 Using solution file: $SLN_FILE"

          # Dotnet build with game path simulation and output folder
          dotnet build "$SLN_FILE" -c Release -o "${OUTPUT_PATH}" /p:DefineConstants="GAMEPATH='${GAME_PATH}'" > "${OUTPUT_PATH}/log.txt" 2>&1 || true

      # 7️⃣ List built DLLs and log
      - name: List output
        run: |
          echo "✅ Built files:"
          ls -la "${OUTPUT_PATH}"
          echo "✅ Log file:"
          cat "${OUTPUT_PATH}/log.txt"

      # 8️⃣ Commit and push output + log
      - name: Commit and push build results
        run: |
          git fetch origin main
          git checkout main
          git pull origin main
          git add "${OUTPUT_PATH}" || echo "Nothing to add"
          git commit -m "Update build output and log [skip ci]" || echo "Nothing to commit"
          git push origin main
